# CLAUDE.md

이 파일은 이 저장소에서 작업하는 Claude Code에게 주는 프로젝트별 참고 사항입니다.

## run.bat / .bat 파일 수정 시 주의: 괄호 이스케이프

`run.bat`(및 `setup-python.bat`)의 `if ... ( ... )` 또는 `for ... ( ... )` 같은
**괄호로 묶인 블록 안에서 `echo`로 출력하는 텍스트에 괄호 `(` `)`가 들어가면 반드시
`^(` `^)`로 이스케이프**해야 합니다.

이유: cmd.exe는 조건이 참/거짓인지와 무관하게 블록 전체를 미리 괄호 균형으로 파싱합니다.
`echo` 텍스트 안의 이스케이프되지 않은 `)`(예: `1)`, `(no install needed):`)가 있으면
파서가 블록이 거기서 끝난 것으로 오인하고, 그 뒤에 남은 텍스트(`:` 나 `On an internet...` 등)를
새 명령으로 잘못 해석해 `X was unexpected at this time.` 오류를 내며 **배치파일 전체가 그
자리에서 즉시 종료**됩니다. 콜론(`:`)이 이스케이프 안 된 `)` 뒤에 오면 특히 잘 걸립니다.

실제로 2026-09-14에 `run.bat`의 "Python not found" 안내 블록에서 `Offline option (no install
needed):`, `1) ...`, `2) ...`, `3) ...` 줄들이 이스케이프 안 되어 있어, 번들 Python이 정상적으로
있는 정상 실행 경로에서도(해당 if 블록 조건이 거짓이라도) 파싱 단계에서 스크립트가 죽어
브라우저/서버 창이 전혀 뜨지 않는 문제가 있었습니다 (수정: 커밋 `7b6bbc0`).

**체크리스트**: `run.bat`/`setup-python.bat`의 괄호 블록 안에 새 `echo` 줄을 추가하거나 기존
줄을 수정할 때는, 그 줄에 `(` `)` `:` 조합이 있는지 확인하고 있다면 `^(` `^)`로 이스케이프하세요.
의심되면 실제로 `cmd /c run.bat`을 실행해 파싱 오류가 없는지 확인합니다 (전체 조건 분기를
각각 테스트: 번들 Python 있음/없음 두 경우 모두).

## Git 커밋 계정

이 저장소는 로컬(repo-local) git 설정에 `user.name=leeyonsei78`, `user.email=leeyonsei78@gmail.com`
이 지정되어 있습니다 (글로벌 설정 아님). 커밋 시 이 계정 정보를 사용합니다.
