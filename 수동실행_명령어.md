# 수동 실행 명령어 모음 (run.bat 이 안 될 때)

`run.bat` 더블클릭이 안 되거나 창이 바로 닫힐 때, 아래 명령어를 **명령 프롬프트(cmd)** 에
직접 입력해서 프로그램을 실행할 수 있습니다. 프로그램 자체는 정상 동작하므로, 이 방법으로
언제든 사용할 수 있습니다.

---

## 0. 준비 — 명령 프롬프트(cmd)를 프로그램 폴더에서 열기

1. 파일 탐색기로 프로그램 폴더 열기 (예: `C:\kiro-azure-policy-offline-auditor`)
2. 폴더 위쪽 **주소창**을 마우스로 클릭 (경로가 파랗게 선택됨)
3. 거기에 `cmd` 라고 입력하고 **Enter**
4. → 그 폴더 위치에서 검은 명령창(cmd)이 열립니다. 아래 명령들을 여기에 입력하세요.

> 아래 명령의 `python\python.exe` 는 폴더에 동봉한 Python을 사용한다는 뜻입니다.
> 만약 이 PC에 Python이 정식 설치돼 있다면 `python\python.exe` 대신 `python` 만 써도 됩니다.

---

## 1. 웹 화면으로 실행 (권장)

```
python\python.exe -m auditor --web
```

- 실행하면 이런 메시지가 나옵니다:
  ```
  웹 UI: http://127.0.0.1:8080  (Ctrl+C로 종료)
  폐쇄망 전용 · 외부 네트워크 연결 없음
  ```
- 이 창은 **그대로 켜 두고**, 인터넷 브라우저(Edge/Chrome)를 열어 주소창에 입력:
  ```
  http://127.0.0.1:8080
  ```
- 화면이 뜨면 Azure 정책 텍스트를 붙여넣거나 파일을 불러와 **보안검토 실행**.
- 종료: 이 명령창에서 **Ctrl + C** (또는 창 닫기).

### 포트를 바꾸고 싶을 때(8080이 이미 사용 중이면)
```
python\python.exe -m auditor --web --port 8090
```
→ 브라우저 주소도 `http://127.0.0.1:8090` 으로 접속.

---

## 2. 명령창에서 바로 파일 검토 (웹 없이)

정책을 파일로 저장해 두었다면, 리포트를 텍스트로 바로 볼 수 있습니다.

```
python\python.exe -m auditor policy.txt
```
- `policy.txt` 자리에 실제 파일 이름을 넣으세요(같은 폴더에 있어야 함).
- 다른 폴더의 파일이면 전체 경로: `python\python.exe -m auditor "C:\경로\policy.txt"`

### JSON 형태로 저장
```
python\python.exe -m auditor policy.txt --json > result.json
```

### 점검하는 ISMS-P 항목 목록만 보기
```
python\python.exe -m auditor --controls
```

---

## 3. 8080 포트가 이미 사용 중일 때 (포트 충돌 해결)

`run.bat`이 바로 닫히는 흔한 원인은 **이미 서버가 켜져 있어 포트가 겹치는 것**입니다.

### 8080을 쓰는 프로세스 확인
```
netstat -ano | findstr :8080
```
- 결과 맨 끝 숫자가 프로세스 ID(PID)입니다. 아무것도 안 나오면 포트는 비어 있음.

### 그 프로세스 종료 (PID를 위에서 확인한 값으로 교체)
```
taskkill /PID 12345 /F
```

### 또는 실행 중인 서버 창에서 Ctrl + C (가장 간단)
서버가 돌고 있는 명령창을 클릭하고 **Ctrl + C** 를 누르면 종료됩니다.

---

## 4. Python 이 동작하는지 점검 (문제 진단용)

```
python\python.exe --version
```
→ `Python 3.11.9` 처럼 나오면 정상. 오류가 나면 `python\` 폴더(동봉 Python)를 다시 준비해야 합니다.

```
python\python.exe -m auditor --controls
```
→ ISMS-P 17개 항목이 나오면 프로그램 정상. 여기서 오류가 나면
`python\python311._pth` 파일 맨 아래에 `..` (점 두 개) 한 줄이 있는지 확인하세요.

---

## 자주 쓰는 명령 한눈에

| 하고 싶은 것 | 명령 |
|---|---|
| 웹 화면 실행 | `python\python.exe -m auditor --web` |
| 웹 화면(다른 포트) | `python\python.exe -m auditor --web --port 8090` |
| 파일 검토(텍스트) | `python\python.exe -m auditor policy.txt` |
| 파일 검토(JSON 저장) | `python\python.exe -m auditor policy.txt --json > result.json` |
| ISMS-P 항목 목록 | `python\python.exe -m auditor --controls` |
| Python 동작 확인 | `python\python.exe --version` |
| 8080 포트 확인 | `netstat -ano \| findstr :8080` |
| 프로세스 종료 | `taskkill /PID <번호> /F` |

> Python이 정식 설치된 PC라면 위의 `python\python.exe` 를 모두 `python` 으로 바꿔 쓰면 됩니다.
