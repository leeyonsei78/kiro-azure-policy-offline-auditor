# 보안 설계 및 검토 문서 (금융권 내부망 반입용)

이 문서는 본 도구를 **금융회사 내부망(폐쇄망 포함)** 에 반입·운영하기 전, 보안 검토자가
확인해야 할 사항을 정리한 것입니다. 코드 기준 최신 상태를 반영합니다.

---

## 1. 구성 요약

| 구성 | 실행 위치 | 외부 통신 |
|------|-----------|-----------|
| `auditor/` (수동: 웹 UI/CLI) | 로컬 PC/서버 | **없음** (완전 오프라인) |
| `autoauditor/` (자동: 스케줄) | 서버 cron / AWS Lambda / Azure Functions | **Slack Webhook 전송만**(선택, 설정 시) |

- 분석 엔진·리포트·통계·화면은 **Python 표준 라이브러리만** 사용(외부 패키지·CDN·AI API 없음).
- 자동 수집은 클라우드 CLI(`aws`/`az`) 또는 SDK(`boto3`/`azure-*`)를 사용(클라우드 배포 시).

## 2. 외부 통신 (Egress)

- **수동 도구**: 외부 통신 **전혀 없음**. 입력은 서버 메모리에서만 처리되고 디스크에 저장하지 않음.
- **자동 도구**: **Slack Incoming Webhook(HTTPS) 1건**만 발신. 그 외 외부 전송 없음.
  - Webhook URL은 **환경변수/배포 파라미터로만** 주입(코드 하드코딩 없음).
  - Webhook 미설정 시 알림은 조용히 생략(전송 시도 자체가 없음).
- 웹 UI는 인터넷 리소스(폰트/스크립트/CDN)를 불러오지 않음(자체 완결 HTML).

## 3. 명령 실행 / 인젝션 방지

- **`shell=True`를 사용하지 않음.** 모든 외부 명령은 `shlex`로 토큰화해 셸을 거치지 않고
  프로그램을 직접 실행(`subprocess`, shell 미사용) → 셸 인젝션 표면 없음.
- 파이프(`|`) 명령은 셸이 아니라 파이썬에서 stdin/stdout으로 단계 연결.
- 자동 차단(remediation) 실행 시, 명령에 셸 메타문자(`| & ; > < \` $()`)가 있으면
  **자동 실행을 거부**하고 수동 검토로 넘김.
- `eval`, `exec`, `os.system`, `pickle` 등 위험 함수 **미사용**.

## 4. 웹 UI 노출

- 기본 바인딩은 **`127.0.0.1`(로컬 전용)**. 외부 주소(`0.0.0.0` 등)로 바인딩하면
  기동 시 경고를 출력함.
- 웹 UI에는 **인증이 없음** → 신뢰된 로컬/폐쇄망에서만 사용 권장.
  다중 사용자·외부 노출이 필요하면 별도 인증·역프록시를 앞단에 두어야 함.
- 웹 UI 서버는 `subprocess`를 실행하지 않음(사용자 입력이 명령 실행으로 이어지지 않음).

## 5. 데이터 처리 / 개인정보

- 입력(정책·구성·로그)은 **메모리에서만** 처리. 수동 도구는 파일로 저장하지 않음.
- 자동 도구는 리포트를 **지정한 출력 폴더**(기본 로컬, Lambda는 `/tmp`)에 저장 →
  민감 데이터가 남을 수 있으므로 **출력 폴더 접근통제·보존정책**을 조직 기준으로 적용할 것.
- 개인정보/시크릿 탐지 결과는 **마스킹**되어 표시(원문 노출 최소화).

## 6. 권한 (최소권한)

- 점검/수집은 **읽기 전용 권한**으로 충분(AWS: SecurityAudit/ViewOnlyAccess,
  Azure: Reader/Security Reader). 배포 템플릿이 읽기 전용만 부여.
- **자동 차단(auto)** 을 실제로 사용할 때만 대상 리소스 수정 권한이 추가로 필요.
  기본값은 **반자동(suggest) + dry-run** 이라 실제 변경을 하지 않음.

## 7. 자동 차단 안전장치

- 기본 모드 `suggest`: 차단/조치 **명령만 생성**(실행 안 함).
- `dry_run=true`(기본): `auto` 모드라도 **실제 변경 없이** 명령만 기록.
- **화이트리스트**(`PROTECT_TAGS`): 보호 키워드가 포함된 대상은 절대 건드리지 않음.
- **롤백 명령 동봉**, **파괴적 작업(삭제/종료) 자동 실행 금지**.

## 8. 비밀정보 관리

- Slack Webhook, 구독/계정 ID 등은 **환경변수·배포 파라미터·앱 설정으로만** 주입.
- 코드·저장소에 하드코딩된 시크릿 없음(정기 grep 스캔으로 확인).
- 로그 출력 시 Webhook 등 비밀값은 "설정됨"으로 마스킹.

## 9. 의존성

- 핵심 엔진: **의존성 없음**(표준 라이브러리).
- 자동/클라우드: `boto3`(AWS Lambda 기본 포함), `azure-*`(Azure Functions 배포 시 설치).
  → 폐쇄망 서버 cron 운영 시에는 CLI만 있으면 SDK 없이도 동작(자동 폴백).

## 10. 반입 전 권장 점검(체크리스트)

- [ ] 사내 정적분석(예: Bandit, Semgrep)으로 스캔 — `shell=True`/`eval` 없음 확인
- [ ] 출력 폴더(`AUTOAUDITOR_OUTPUT_DIR`)에 접근통제·보존정책 적용
- [ ] Slack Webhook URL을 비밀 저장소(환경변수/시크릿 매니저)로 주입
- [ ] 웹 UI는 `127.0.0.1`로만 기동(또는 인증·역프록시 적용)
- [ ] 자동 차단은 `suggest`+`dry_run`으로 시작, 화이트리스트 등록 후에만 상향
- [ ] 클라우드 권한은 읽기 전용으로 시작

---

취약점 제보나 문의는 저장소 이슈로 남겨주세요. 실제 조치 전에는 대상 환경과 업무 요건을
확인하시고, 본 도구는 **참고용 자동 검토**입니다.
